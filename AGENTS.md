# Video Mixed Clip 仓库规则

1. 本仓库只负责本地视频素材混剪、时间线返修和剪映可编辑交付。
2. 禁止加入短剧剧本、人物资产、场景资产、分镜和生成式视频控制器代码。
3. 原始视频、配音和文案只读，不删除、不移动、不覆盖。
4. 每个原视频默认只处理 0 到 min(duration, 40.0) 秒。
5. 40 秒后的内容不得进入镜头、缩略图、向量、候选、时间线或编辑包。
6. 本机绝对路径只允许写入被 Git 忽略的本地配置。
7. Whisper 只提供时间证据，不得覆盖用户原文。
8. 剪映直接草稿是可选入口，标准编辑包必须始终可生成。
9. 原声、配音和字幕必须保持独立可编辑。
10. 修改后至少运行 `python -m pytest -q` 和 `video-mixed-clip --help`。
