# Video Mixed Clip 仓库规则

1. 本仓库只负责信息流素材理解、自动混剪、素材裂变、时间线返修和剪映可编辑交付。
2. 禁止加入短剧剧本、人物资产、场景资产、分镜和生成式视频控制器代码。
3. 原始视频、配音和文案只读，不删除、不移动、不覆盖。
4. 素材识别默认覆盖整条视频，不得用成片使用限制截断识别范围。
5. 同一来源在成片中的使用时长、比例和重复间隔必须由剪辑规则控制。
6. 任何产品零售包装、运输包装或疑似容器默认硬淘汰，不得只做降分。
7. 拍摄道具必须与产品包装分开标注；无法确认时按疑似包装处理。
8. 原字幕与新文案冲突时，优先裁切、局部模糊或小面积磨砂；禁止大色块覆盖。
9. 跑量母素材裂变必须保存父子关系、固定片段、变化维度、测试假设和随机种子。
10. 每轮裂变尽量只改变一到两个变量，禁止无目的随机换镜头。
11. 文案允许为信息流效果重写，但不得篡改关键事实或生成无法证明的承诺。
12. 明示含义、暗示含义和表现手法必须分开记录，并保留证据与置信度。
13. 本机绝对路径只允许写入被 Git 忽略的本地配置。
14. Whisper 可以提供时间证据和原素材对白识别；用户原文不再是唯一允许输出的字幕版本。
15. 剪映直接草稿是可选入口，标准编辑包必须始终可生成。
16. 原声、配音和字幕必须保持独立可编辑。
17. 修改后至少运行 `python -m pytest -q`、`video-mixed-clip --help`、`material-intelligence --help` 和 `creative-variants --help`。
